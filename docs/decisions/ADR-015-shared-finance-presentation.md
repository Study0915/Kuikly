# ADR-015 · 共享视觉与可核对证据的呈现边界

2026-09-13，Accepted。用户明确要求实施浅色专业行情改版和课程提交准备。以 `485964c` 为基线，Windows Codex 主代理是唯一写入者，评审代理只读。

改版在现有 shared/ui、insight 和 H5/Android 宿主完成，不新增框架或 Gradle module。统一主题、字体和按钮；QuoteEvidenceLens 重排为图表、依据、当前事实、按需展开计算。结论—区间—交易日双向核对、缺失样本使相关计算失效、两题真实复用是主线，不扩实时数据、模型或新平台。

呈现状态与领域焦点分离：Pager 按 DocumentKey 保存计算展开和详情滚动位置，演示面板仅属于当前页面。LensFocus 仍由 FinanceSession/LensState 管理，控件选中态从既有投影派生。旧视图的恢复回调必须匹配请求及挂载代次。聊天继续使用 ChatController 的草稿、滚动锚点和逐卡展开状态。

H5 最大宽度480，宿主以实际容器和可视高度通知Kuikly；Android使用平台字体与安全区。保留图表绘制/命中几何，视觉调整不能削弱数值、路由和触控验收。构建、UI、录像和新候选绑定同一版本，旧r3保留。仅准备本地成果和官方登记文件，不推送、合并或建PR。
