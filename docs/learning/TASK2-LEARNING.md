# Task 2 项目复盘

## 这次视觉改版学到了什么

页面首先要让用户知道“现在看的股票、价格和依据是什么”。本轮把调试场景放进演示设置，公式默认折叠，完整双图移到首屏；问答业务卡用三条紧凑依据和一个主要详情动作，保持两题视觉一致。信息减少的是重复的边框和展开内容，数值口径、风险、时效与可用性边界仍保留。

展示状态和业务状态分开：展开公式与滚动偏移按文档保存；选择哪条依据继续由已有模型控制。浏览器实际布局完成后恢复偏移，并检查页面代次，避免旧回调串页。320/390/430/1024 和短视口检查不能替代原生设备/键盘测试。

讲述时先操作“结论→区间→交易日→公式”，再展示缺量失效和另一题复用；不要先报测试数量。最新视频与[评分证据审计](../submit/SCORING-AUDIT.md)未沿用旧的 90/91 自评。下一步：按[新版演示](../demo/README.md)练习一遍，由本人确认掌握程度。


2026-09-13。材料依据[TESTS](../REVIEWS/TASK2-TESTS.md)和真实实现核对；个人是否能独立讲述仍需本人练习，代理不代签。

## 30 秒介绍

这是用 Kuikly 做的股票问答原型。用户提问后，回答既有 Markdown 摘要，也有能核对的行情证据卡。点击某条依据可以进入对应股票、快照和区间的详情，再返回原会话继续追问。首版用确定性 Mock，主要解决“AI 信息如何被理解和核对”，而不是证明模型预测能力。

## 职责与路径

用户提出约90分目标、工作区安装与自主推进约束；Windows Codex完成方案选择、业务实现、测试、文档和本地交付准备。应如实说明AI编程协作，不把代理写的每一行都说成独立手写。

体验路径：输入“比较 A 和 B”→Markdown说明口径→A/B各自证据卡→点击B的局部反弹→详情选中+5.49%→返回原会话→追问B风险→检验B缺量→倍数不可用但价格依据仍在。

## 架构和五个关键选择

1. **内容先分类型。** Markdown和EvidenceCard分别建模。实体标识和行情快照随业务块传递，不从自然语言里猜路由。因此格式变化不会把用户送到另一只股票。
2. **一条请求一张票据。** 票据包含代次、消息ID、原问题、上下文和场景。取消、新会话让旧票据失效，重试保留原位置和上下文。消息列表不会因迟到响应串线。
3. **真实复用现有能力。** 聊天卡复用Task1的EvidenceResolver和LensPresenter，展开走势复用MarketPlot，详情复用FinanceDetail/QuoteEvidenceLens。单股票、A/B比较和缺量共用EvidenceAnswerCard。
4. **会话与视图分开。** Pager持有ChatController/ChatSession；离开聊天看详情不会销毁记录。卡片展开状态以消息ID和快照键保存，避免A/B或两条回答相互影响。
5. **有边界地支持Markdown。** 只渲染受限文本块和样式；不执行HTML、不加载图片、不打开外链。JVM单测之外还要看浏览器：本轮确实发现了JS Unicode正则与JVM接受范围不同的问题并修复。

## 评分亮点与验证

| 维度 | 可讲述的成果 | 证据边界 |
|---|---|---|
| 功能40 | 输入/记录/Markdown+卡片/详情/返回/重试/取消 | Task2 H5 39项通过 |
| 工程25 | typed blocks、代次隔离、同一卡片与Task1组件复用 | Task2共21个JVM测试；全仓53个 |
| AI25 | 结论依据时效风险、带实体追问、缺量情景 | Mock交互真实运行，非真实模型 |
| 加分10 | 320/390/430/1024布局、桌面输入、20轮会话、触摸与恢复 | 11项会话/手势；桌面另39项；新增17项在触摸与桌面各通过；设备未验证 |

H5 production、Android Debug APK、JS/Android页注册检查通过；Task1全部110项浏览器检查回归通过。构建有锁定工具链提示，不声称零警告。设备/iOS/鸿蒙与真实API不申报。

## 简历表述候选

- 在Codex协作下完成Kuikly股票问答原型，以Markdown与结构化证据卡承载回答，实现问题→行情依据→精确详情→返回追问的闭环。
- 将行情事实解析、卡片呈现与会话请求状态分离，复用Task1的行情和图形组件，覆盖A/B比较、缺量降级与取消重试。
- 建立53项共同逻辑测试与分平台H5验收，修复错误比较对象、快捷追问丢失草稿及动态列表恢复时序；如实区分Mock、浏览器运行与APK构建。

## 十个面试问题

**1. 和普通聊天框区别？** 不只读文字，用户可以直接核对依据对应的行情、时间与计算式，并从同一实体继续追问。

**2. 真实用了AI模型吗？** 没有。提供确定性规则模板与Mock数据，重点证明AI信息载体与业务交互。接模型需要遵守typed内容合同和事实校验。

**3. 为什么不用HTML渲染Markdown？** 会引入脚本、远程图片和链接行为。当前文本样式已满足题面和演示，受限renderer跨端更可控。

**4. 如何避免旧结果覆盖新会话？** 请求必须等于当前pending票据；取消/reset后旧票据失效，即便计时回调还运行也不会更新记录。

**5. 重试会重复提问吗？** 不会。重试替换失败回答，保留消息ID、问题和股票上下文，只更新代次与attempt。

**6. 为什么不保存到本地？** 首版没有登录或云同步需求；生命周期内状态已经支持来回查看，明确刷新清空能避免伪装成持久会话。

**7. 如何证明可复用？** A/B同屏调用相同卡片，缺量走同一组件；Task1详情与Task2卡片消费同一事实层，展开图共用MarketPlot，有运行与单测证据。

**8. 比较的口径是什么？** 相同20日窗口，各自起点计算涨跌幅、各自此前五日均量计算倍数。不能把较高价格或较大倍数直接理解成更好投资。

**9. 最有价值的失败是什么？** JVM通过但浏览器正则报错，以及vfor内两个根节点导致运行失败。这证明编译/单测不能替代真实UI链路。

**10. 下一步真正需要做什么？** 用户个人练习讲述；若扩展平台，先保持工作区隔离验证Android设备与键盘，再决定是否接真实服务。当前不自动发布。

## 持续优化中值得讲清的取舍

“比较 C”曾被默认补成A/B。界面可以正常运行，结果却没有忠实回答用户。现在对象不足或超范围时先提示；日期和数量按完整token识别，避免误拒合法A股票。这说明AI信息载体还必须让输入、证据和跳转对象保持一致。

快捷追问和输入框发送是不同操作：前者发出卡片自己的问题，不能顺带删除用户草稿。会话保存还不等于阅读位置稳定；返回后的布局、旧计时回调、新会话与原位重试需要分别处理。

交付也有版本问题。ZIP校验只能说明文件没有拷坏；当前流程进一步绑定源码输入、已构建JS/APK、浏览器检查和录像。这样更新代码后不能继续把旧演示包标成已验收。所有校验与反例都在工作区进行，不依赖全局环境。

## 已知限制

最多500字、20轮；仅示例股票A–L与受限意图，不理解任意金融问题。Markdown不是完整CommonMark。会话刷新清空。H5视口检查不是手机键盘运行。评分是内部自评，实际老师评分未知。

下一步：结合两题最新演示和评分证据准备本地候选；个人复述能力未验证。
