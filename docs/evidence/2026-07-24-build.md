# 2026-07-24 构建证据

本记录只保留已实际执行的本地结果。命令均在 `E:\Internship\tecent\Kuikly` 执行，缓存位于工程 `.cache`。

| 时间 | 命令 | 结果 | 说明 |
|---|---|---|---|
| 2026-07-24 | `:KuiklyChart:jsNodeTest` | 通过 | Kotlin/JS 编译、测试编译与 `jsNodeTest` 均成功。覆盖 `ChartMathTest` 与 `CandleMathTest`。 |
| 2026-07-24 | `:shared:compileKotlinJs` | 通过 | 包含 KSP、Mock Provider、行情页面与 K 线详情页。 |
| 2026-07-24 | `:h5App:jsBrowserProductionWebpack` | 通过 | production bundle 生成成功；Webpack 提示 `h5App.js` 约 429 KiB 的性能建议，非构建失败。 |
| 2026-07-24 | H5 本地静态服务 + 应用内浏览器 | 通过 | 首页行情/折线图实际渲染；点击“贵州茅台”跳转至 `?page_name=stock_detail&code=600519`，详情 K 线/成交量数据与 Mock AI 解读均实际渲染。 |
| 2026-07-24 | `:shared:jsTest` | 未通过 | 测试代码完成编译，但 Kotlin/JS 后端在 `compileTestDevelopmentExecutableKotlinJs` 抛出 `IrSimpleFunctionSymbolImpl is already bound`；禁用增量与清理 `shared` 构建目录后可复现，非断言失败。 |
| 2026-07-25 | `:shared:testDebugUnitTest` | 通过 | Android 本地 JVM 运行 `MockProvidersTest` 的 5 个用例，`failures=0`、`errors=0`；覆盖离线 fixture、详情/趋势/K线、未知代码、确定性 Mock AI 和加载状态。 |
| 2026-07-25 | `:androidApp:assembleDebug` | 通过 | 使用项目 `.cache/jdk17` 的 JDK 17.0.12、项目 `.cache/android-sdk` 和 Gradle 8.0 分发成功构建。产物为 `androidApp/build/outputs/apk/debug/androidApp-debug.apk`，SHA-256 为 `B16BDC82A11E9923253E73EC274B82EF3770BA9C4888DBEBE1B99D970E0303FF`。 |
| 2026-07-25 | `scripts\verify.ps1` | 通过 | 使用项目内 Gradle Wrapper 8.0、JDK 17、Android SDK 与缓存，连续完成图表 JS 单测、`shared` Kotlin/JS 编译、H5 production bundle、Mock Provider Android 本地 JVM 单测和 Android Debug APK 构建。 |
| 2026-07-25 | H5 详情页本地静态服务 + 应用内浏览器 | 通过 | 指标行、分时图、K线/成交量和 Mock AI 卡片按顺序显示；分时图与 K线点击均实际显示 Tooltip。浏览器自动化拖动未触发 Kuikly `pan`，因此未将滑动运行时标记为通过。 |
| 2026-07-25 | H5 首页柱图本地静态服务 + 应用内浏览器 | 通过 | `BarChart` 实际渲染四项 Mock 股票涨跌幅；点击“五粮液”后显示 `1.72` Tooltip，并更新选择摘要。 |
| 2026-07-25 | H5 首页列表本地静态服务 + 应用内浏览器 | 通过 | 观察列表实际滚动后展示四支股票；点击“贵州茅台”实际跳转至 `?page_name=stock_detail&code=600519`。 |

## 环境边界

- 本机全量 Gradle 可执行文件为 `D:\Gradle\bin\gradle.bat`（9.6.1），与 AGP 7.4.2 不兼容；工程 Wrapper 已固定为实际验证通过的 Gradle 8.0，分发位于项目 `.cache\gradle`。
- JS/H5 构建最初使用 `F:\jdk-21`；Android APK 构建已改用项目 `.cache/jdk17` 的完整 JDK 17.0.12。
- H5 浏览器实际交互与 Android Debug APK 构建均已通过。未连接 Android 真机/模拟器，iOS/鸿蒙也未在本文件标记为通过。
- Android 构建会提示 AGP 7.4.2 仅测试至 `compileSdk 33`，但使用 `compileSdk 34` 的本次构建已成功；还会出现不可写 `C:\Users\weila\.android\analytics.settings` 的指标初始化提示。两者均未阻断构建，也不代表 Android 运行已验证。
